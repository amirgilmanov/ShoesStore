<Window x:Class="ShoesStore.EditOrderWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Редактирование заказа" Height="700" Width="900">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0 0 0 10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="250"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Margin="0 0 10 0" VerticalAlignment="Top">
                <TextBlock Text="Адрес пункта выдачи:" Margin="0 5"/>
                <TextBlock Text="Дата заказа:" Margin="0 5"/>
                <TextBlock Text="Дата доставки:" Margin="0 5"/>
                <TextBlock Text="Общая стоимость:" Margin="0 5" FontWeight="Bold"/>
            </StackPanel>

            <StackPanel Grid.Column="1" VerticalAlignment="Top">
                <TextBlock Text="{Binding CurrentOrder.PickUpPointAddress}" 
                           Margin="0 5" TextWrapping="Wrap" 
                           x:Name="pickUpPointAddressInput"/>

                <TextBox x:Name="dateInput"
                         Margin="0 5"
                         Padding="5"
                         Text="{Binding CurrentOrder.OrderDateString, Mode=TwoWay, UpdateSourceTrigger=LostFocus}"/>

                <TextBox x:Name="deliveryDateInput"
                         Margin="0 5"
                         Padding="5"
                         Text="{Binding CurrentOrder.DeliveryDateString, Mode=TwoWay, UpdateSourceTrigger=LostFocus}"/>

                <TextBlock x:Name="totalPriceTextBlock"
                           Text="{Binding CurrentOrder.TotalPrice, StringFormat=C2}" 
                           Margin="0 5" FontWeight="Bold"/>
            </StackPanel>
        </Grid>

        <Separator Grid.Row="0" VerticalAlignment="Bottom"/>

        <Grid Grid.Row="1" Margin="0 10 0 10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="250"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <TextBlock
                Grid.Column="0"
                Text="Изменить статус:"
                Margin="5"
                VerticalAlignment="Center"/>

            <ComboBox
                x:Name="selectedOrderStatus"
                Grid.Column="1"
                ItemsSource="{Binding AllOrderStatus}"
                DisplayMemberPath="Name"
                SelectedItem="{Binding CurrentOrder.OrderStatus, Mode=TwoWay}" 
                Margin="5"/>
        </Grid>

        <Separator Grid.Row="1" VerticalAlignment="Bottom"/>

        <StackPanel Grid.Row="2" Margin="0 10 0 0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="Состав заказа:" FontWeight="Bold" Margin="0 0 0 5" Grid.Column="0"/>
                <Button Content="Добавить товар" Click="AddOrderDetailClick" Width="150" HorizontalAlignment="Right" Grid.Column="1"/>
            </Grid>
            <DataGrid x:Name="orderDetailsDataGrid"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False" 
                      ItemsSource="{Binding CurrentOrder.OrderDetailsViewModels}"
                      RowEditEnding="OrderDetailsDataGrid_RowEditEnding"
                      Height="Auto">
                <DataGrid.Columns>
                    <DataGridTemplateColumn Header="Артикул" Width="Auto">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Good.Article}"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                        <DataGridTemplateColumn.CellEditingTemplate>
                            <DataTemplate>
                                <ComboBox ItemsSource="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType=Window}, Path=DataContext.AllGoods}"
                                          DisplayMemberPath="Article"
                                          SelectedItem="{Binding Good, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                          IsEditable="False"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellEditingTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTextColumn Header="Наименование" Binding="{Binding Good.Name}" IsReadOnly="True" Width="*"/>
                    <DataGridTextColumn Header="Цена" Binding="{Binding Good.Price, StringFormat=C2}" IsReadOnly="True" Width="Auto"/>
                    <DataGridTextColumn Header="Количество" Binding="{Binding Quantity, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="Auto"/>
                    <DataGridTextColumn Header="Сумма" Binding="{Binding Amount, StringFormat=C2, Mode=OneWay}" IsReadOnly="True" Width="Auto"/>
                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="10 0" VerticalAlignment="Center">
            <Button Content="Сохранить" Click="SaveButtonClick" Width="150" Margin="10 0"/>
            <Button Content="Удалить" Click="DeleteButtonClick" Width="150" Margin="10 0"/>
            <Button Content="Отмена" IsCancel="True" Width="150" Margin="10 0"/>
        </StackPanel>
    </Grid>
</Window>